{
  "name": "@multifarious/strava-webhook",
  "version": "0.1.0",
  "description": "Strava webhook handler with avalanche forecast enrichment",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && npm run bundle && rm -f dist/lambda/*.js && npm run copy-zones && npm run copy-assets",
    "bundle": "esbuild lambda/ingest.ts lambda/processor.ts lambda/oauth.ts lambda/web.ts --bundle --platform=node --target=node22 --outdir=dist/lambda --external:@aws-sdk/* --format=esm --out-extension:.js=.mjs",
    "copy-zones": "mkdir -p dist/lambda/data/zones && cp ../../packages/forecast-api/data/zones/*.geojson dist/lambda/data/zones/",
    "copy-assets": "mkdir -p dist/lambda/assets && cp -R assets/* dist/lambda/assets/ 2>/dev/null || true",
    "watch": "tsc -w",
    "test": "vitest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.709.0",
    "@aws-sdk/client-sqs": "^3.934.0",
    "@aws-sdk/lib-dynamodb": "^3.709.0",
    "@multifarious/forecast-api": "^0.1.0",
    "@multifarious/forecast-formatter": "^0.1.0",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.2",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.2",
    "source-map-support": "^0.5.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}